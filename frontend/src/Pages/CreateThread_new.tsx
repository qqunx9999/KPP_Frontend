import React from 'react';

const CreateThread_new = () => {
  const temp = {
    "margin": "10px",
  };

  return(
    <div>
      <div className="temp">

      </div>
    </div>
  );
};

export default CreateThread_new;