import React from 'react';

const CreateThread_new = () => {
  const temp = {
    "margin": "10px",
  };

  return(
    <div>
      <div style={ temp }>
        <h1>Create Thread</h1>
      </div>
    </div>
  );
};

export default CreateThread_new;